// Breaking Bad Ducks

// Initial State

const initialState = {
  characters: [],
  loading: true
};

// Action Types Go Here

// Reducer
export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
